# PARADOX — new independent prototype

This directory contains a standalone plain HTML/CSS/JS build with no vendor-generated-project badge or runtime branding.

It uses Wikimedia's public API from the browser for reference lookup. The current prototype deliberately keeps the assessment conservative and transparent; it is not a replacement for the original PARADOX engine tasks 1–15.

Open index.html locally, or publish the paradox-new directory through GitHub Pages.